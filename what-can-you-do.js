const prompt = require ('prompt sync') ();
const age = Number(prompt('Please enter your age: '));
if (age < 16){
    console.log('You can not drive');
        }else if (age >= 16) {
        }else if (age >= 18) {
        }else { 
            console.log ('You cann do anything!')
        }